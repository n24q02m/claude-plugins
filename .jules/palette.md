## 2024-06-09 - Add alt text to hero image\n**Learning:** The Starlight `index.mdx` splash page hero section allows defining an `alt` property directly underneath `image.file` to provide accessible text for the hero image, solving an a11y defect where screen readers would receive no context.\n**Action:** Always verify hero sections in `astro` splash pages explicitly define the `alt` property within the `image` configuration object.
